# These are files generated by running asciidoc(1) against the *.adoc files
*.html
images/

# This contains public keys that the MMS will consider "administrators".
# It should be safe to put these into a repo, but better safe than sorry.
context/admin-keys
